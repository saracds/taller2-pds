spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D:/ESTUDIO/INTELLIJ/configdatataller2
  security:
    user:
      name: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
server:
  port: 9000

